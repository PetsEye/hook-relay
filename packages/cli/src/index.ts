#!/usr/bin/env node
// `hookrelay` CLI scaffold. Milestone 5 adds `listen` (local tunnel demo) and `trigger` (signed POST).
console.log("hookrelay CLI scaffold — see README for the milestone 5 roadmap.");
console.log("Usage: hookrelay <listen|trigger> [options]");
